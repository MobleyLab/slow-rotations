from setuptools import setup, find_packages

setup(
    name="slow-rotations",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[
        # list runtime dependencies here, e.g. numpy, rdkit
    ],
)
